[environment]
name = "daymark"
description = "Native macOS Daymark app and CLI. The scripts pin the Xcode-beta toolchain this package needs."

[commands]
build = "scripts/build.sh"
test = "scripts/test.sh"
doctor = "scripts/doctor.sh"
